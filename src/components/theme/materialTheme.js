import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {},
    typography: {
        fontFamily: "Poppins",
        h2: {
            fontWeight: "normal",
            fontFamily: "Poppins"
        },
        h4: {
            fontWeight: "normal",
            fontFamily: "Poppins"
        }
    }
});

export default theme